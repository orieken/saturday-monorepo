stream_over_http_enabled: true
server:
  http_listen_port: 3200
  grpc_listen_port: 9095

metrics_generator:
  registry:
    external_labels:
      source: tempo
  storage:
    path: /tmp/tempo/generator/wal
    remote_write:
      - url: http://prometheus:9090/api/v1/write
        send_exemplars: true

distributor:
  receivers:
    otlp:
      protocols:
        grpc:
          endpoint: '0.0.0.0:4317'
        http:
          endpoint: '0.0.0.0:4318'
    jaeger:
      protocols:
        thrift_http:
          endpoint: '0.0.0.0:14268'
    zipkin:
      endpoint: '0.0.0.0:9411'

storage:
  trace:
    backend: local
    local:
      path: /tmp/tempo/blocks

compactor:
  compaction:
    block_retention: 48h
#server:
#  http_listen_port: 3200
#  grpc_listen_port: 9095
#
#distributor:
#  receivers:
#    otlp:
#      protocols:
#        grpc:
#          endpoint: "0.0.0.0:4317"
#        http:
#          endpoint: "0.0.0.0:4318"
#    jaeger:
#      protocols:
#        thrift_http:
#          endpoint: "0.0.0.0:14268"
#    zipkin:
#      endpoint: "0.0.0.0:9411"
#
##server:
##  http_listen_port: 3200  # HTTP query frontend port
##  grpc_listen_port: 9095  # gRPC query frontend port
##  log_level: info
#
##query_frontend:
##  search:
##    duration_slo: 5s
##    throughput_bytes_slo: 1.073741824e+09
##    metadata_slo:
##      duration_slo: 5s
##      throughput_bytes_slo: 1.073741824e+09
##  trace_by_id:
##    duration_slo: 5s
#
##distributor:
##  receivers:                          # this configuration will listen on all ports and protocols that tempo is capable of.
##    otlp:
##      protocols:
##        http:
##        grpc:
#
##ingester:
##  max_block_duration: 5m               # cut the headblock when this much time passes. this is being set for demo purposes and should probably be left alone normally
#
##compactor:
##  compaction:
##    block_retention: 48h                # overall Tempo trace retention. set for demo purposes
#
##metrics_generator:
##  registry:
##    external_labels:
##      source: tempo
##      cluster: docker-compose
##  storage:
##    path: /var/tempo/generator/wal
##    remote_write:
##      - url: http://prometheus:9090/api/v1/write
##        send_exemplars: true
##  traces_storage:
##    path: /var/tempo/generator/traces
#
#storage:
#  trace:
#    backend: local
#    local:
#      path: /var/tempo/traces
